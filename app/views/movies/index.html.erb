<div class="container pb-5">
  <div class="row justify-content-center">
    <div class="col-sm-8 my-3">
      <%= form_with url: movies_path, method: :get, class: "d-flex" do %>
        <%= text_field_tag :query,
              params[:query],
              class: "form-control",
              placeholder: "Type a keyword"
          %>
        <%= submit_tag "Search", name: "", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-sm-8">
      <div id="movies">
        <%# <% if @movies.any? %>
          <%# <% @movies.each do |movie| %>
            <%# <h4><%= movie.title. <small>Directed by <%= movie.director.full_name</small></h4> %>
            <%# <p><%= movie.synopsis</p> %>
          <%# <% end %>
        <%# <% else %>
          <%# <em>Sorry, no movies found.</em> %>
        <%# <% end %>
        <% @results.each do |result| %>
          <h4><%= result.searchable.title %>.</h4>
          <p><%= result.searchable.synopsis%></p>
        <% end %>
      </div>
    </div>
  </div>
</div>
